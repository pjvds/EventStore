box: ami-f7f0ac9e
box-type: ami
build:
  steps:
    - install-packages:
        packages: libv8-dev
    - script:
        name: Copy v8 libs
        code: |
          cp -r $HOME/EventStore/v8 $WERCKER_SOURCE_DIR/
    - script:
        name: build eventstore
        code: |
            ./build.sh incremental
